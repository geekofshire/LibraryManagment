extends layout 

block content 
    h1=title
    ul
        each author in author_list
            li 
                a(href=author.url) #{author.name}
                | (#{author.get_birth_Date}-#{author.get_death_Date})
            
        else
            li There are no authors